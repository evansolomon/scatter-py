#!/usr/bin/env python

import scatter
import os
import sys

# Do it
deploy_dir = scatter.get_deploy_dir(True)

# Remove the first item, which is the script name
del sys.argv[0]
os.system('cd ' + deploy_dir + ' && cap ' + sys.argv[0])
